# Sesión 12-06-2026 — Instrucciones para continuar

## Rama de trabajo
**Rama activa hoy: `dev-12062026`**  
NO trabajar en `main`. Todos los commits van a `dev-12062026`.  
Al final del día, el usuario hará merge a `main` para un único deploy en Netlify.

## Reglas obligatorias
1. `git checkout dev-12062026` antes de tocar nada.
2. Commits en `dev-12062026`, nunca en `main`.
3. Añadir `[skip ci]` al mensaje de commit si el cambio NO necesita pasar por GitHub Actions (docs, estilos menores, refactors sin lógica). Para fixes de bug o features, NO añadir `[skip ci]`.
4. NO hacer `git push` a `main` sin que el usuario lo autorice explícitamente.
5. Tras cada tarea: `npx tsc --noEmit` + `npx vitest run` limpios.

## Estado de commits en la rama (al cerrar sesión)
- `b2eb426` feat: CORS explícito en rutas /api
- `9bfe327` feat: PWA manifest.json
- `ca76bcc` fix: force-dynamic en market/pulse + DATABASE_URL postgres en CI
- `b77bcf8` backup: estado pre auditoría 12062026
- `997cd3a` fix: usar logger en avisos de entorno y error boundary

## Tareas pendientes del plan (para esta rama)

### Pequeñas (empezar por aquí)
- **9.2** — Estado vacío de búsqueda con sugerencias  
  Archivo: `src/components/GameGrid.tsx`  
  Cuando `isFiltered && games.length === 0`: mostrar mensaje "Sin resultados para «X»", botón "Limpiar búsqueda", y 4 productos sugeridos (los de mayor `discountPercent` del array completo que llega por props).

- **G1** — Lighthouse CI en GitHub Actions  
  Instalar `@lhci/cli` como devDep. Crear `.lighthouserc.js`. Añadir job `lighthouse` en `.github/workflows/ci.yml` que corra tras el build. Ver detalles en `docs/PLAN-MEJORAS-AUDITORIA.md` sección G1.

### Grandes (requieren sesión dedicada)
- **C1** — Audit log table (Prisma + 15 puntos instrumentados)
- **C2 / 10.2** — E2E Playwright (3 specs completos)

## Referencia
Plan completo: `docs/PLAN-MEJORAS-AUDITORIA.md`
